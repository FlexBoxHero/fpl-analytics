#!/usr/bin/env bash
# Seed the database with initial data.
# Usage: ./scripts/seed.sh
# Implementation: Phase 06+ (after schema and seed scripts exist).

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

echo "Seed script placeholder — implement in Phase 06+ once postgres/seeds/ is populated."
exit 0
