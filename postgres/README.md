# PostgreSQL

Database migrations and seed data for the FPL analytics platform.

- `migrations/` — schema migration scripts (Phase 06)
- `seeds/` — initial/reference data (Phase 06+)

Connection settings are defined in the root `.env` file.
